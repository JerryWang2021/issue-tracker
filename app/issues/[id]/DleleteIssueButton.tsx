import { Button } from '@radix-ui/themes'

const DleleteIssueButton = ({ issueId }: { issueId: number}) => {
  return (
    <Button color="red">Delete Issue</Button>
  )
}

export default DleleteIssueButton
